# Core packages — install with:
#   venv\Scripts\pip install -r requirements.txt

aiohttp
aiohttp-socks
python-socks
requests
rich

# Optional browser-helper packages live in requirements-browser.txt (NOT needed for the API service):
#   venv\Scripts\pip install -r requirements-browser.txt
#   venv\Scripts\python -m cloakbrowser install
#   venv\Scripts\python -m camoufox fetch
#   venv\Scripts\playwright install chromium
